# 核心依赖
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
pydantic>=2.6.0

# 系统监控
psutil>=5.9.0

# NVIDIA GPU 监控（可选，没有 NVIDIA 也能跑）
nvidia-ml-py>=12.535.108; platform_system == "Linux" or platform_system == "Windows"

# 可选：LangGraph（进阶阶段深度使用，MVP 先不强制）
# langgraph>=0.1.0
# langchain>=0.2.0