# LocalFlow AI · v0.1.0 测试版发布计划

> 目标：尽快上架 GitHub 第一个测试版。路线 = **本地模型 + dsh 为主、云端兜底**；产物 = **四路安装包（win / mac / linux）**；当前状态 = 先确认计划，动手待拍板。
> 本机现状（已探测）：Ollama 在跑（已下 `qwen2.5vl:7b`）✅；dsh 已在 8080 监听（PID 88279，node）✅；git remote 为空 ❌。

---

## 一、发布前必做（P0，阻塞发布）

### 1. 安全链（已逐行验证，必须修）
- **问题**：后端监听 `*:8765`；CORS `allow_origins=["*"]` + `allow_credentials=True`（Starlette 走 `cors.py:167` 反射任意 Origin）；默认免鉴权；Agent 有 11 工具含文件/命令。
- **改法**：
  - `main.py` 启动绑定 `127.0.0.1`（CLI `serve` 已是 127.0.0.1，问题在 PyInstaller/手动启动时显式 `--host 0.0.0.0`，统一收紧）
  - CORS 改为白名单 `allow_origins=["http://127.0.0.1:8765","http://localhost:8765"]`，去掉 `allow_credentials`（或只在带 API Key 时开）
  - `openai-api-guide.html:237` 的"只监听 127.0.0.1"改为与实际一致，并加安全提醒
- **文件**：`backend/localflow/main.py`、`deps.py`/`run_server.py`、`api/openai.py`、`openai-api-guide.html`

### 2. 白屏 / dsh 接入（致命，核心体验）
- **问题**：`main.js` 只拉 Python 后端，从不拉/探 dsh；`index.html:1082` 无脑设 iframe 到 8080 → 打包后对话页白屏。
- **改法（两轮）**：
  - 优先：`main.js` 增加 dsh 健康探测（同 8765 的轮询模式）；`index.html` 在 8080 不可达时渲染"引导卡"（显示如何安装/启动 dsh、或一键 `npx @deepseek-ai/dsh`），**绝不白屏**。
  - 理想：`main.js` 在打包态也 spawn dsh 子进程（参考后端 spawn），让安装包自包含对话能力。

### 3. 仓库卫生
- `backend/runtime_data/backend.log`（792KB）入版本库 → 加 `.gitignore`
- `api/attach.py` 用 `pypdf`，但 requirements/pyproject/spec 三处漏声明 → 补上（否则打包后 PDF 解析必 `ImportError`）
- `dsh-poc/dsh-home/profiles/*/package.json` 写死 `/Users/zhanghaoran/...` 绝对路径 → 改相对/可移植，否则换机/CI `pnpm install` 失败
- `pnpm-workspace.yaml` 的 `allowBuilds` 是占位 `set this to true or false` → 填真值（koffi/node-pty 需原生编译）
- 补 `CONTRIBUTING.md`、Issue 模板、PR 模板（D4 清单项，当前缺失）
- 建 git remote，准备 push

### 4. 构建链路（四路安装包跑通）
- 后端 PyInstaller 补 `pypdf`，验证 macOS arm64 产物
- **dsh 打包策略（唯一需你拍板的硬决策，见第三节）**
- `electron-builder.yml` 校验 `extraResources: ../backend/dist/localflow-backend → backend` 路径；win/linux target 确认
- `build.yml` CI：建议补 lint + 跑 `tests/`（当前零测试步骤），验证四路 artifact 产出

---

## 二、建议同步修（P1，不影响上架但影响口碑）

- `index.html:4895/4955/4961/4974` 相对路径 `fetch('/api/...')` 在 `file://` 下必挂 → 硬件缓存状态、Agent 产物预览全挂，统一改用 `API` 前缀
- `main.js:20` `isBackendUp` 把 4xx 当在线 → 收紧判定
- 首启向导 600ms 弹窗 vs 后端就绪 15s 错配；且 `lf_wizard_seen` 只弹一次
- API Key 持久化 `PermissionError` 却显示绿色"成功" → 改提示与样式

## 三、唯一硬决策：dsh 怎么进安装包

| 方案 | 做法 | 工作量 | 跨平台风险 |
|---|---|---|---|
| **A. 全平台引导（推荐 v0.1.0）** | 安装包只内嵌 Python 后端；对话页检测到 8080 不可达时显示"一键安装/启动 dsh"引导（Mac: `npx @deepseek-ai/dsh`；win/linux 同）。dsh 由用户环境提供 | 小 | 低，四路安装包都能"跑通"（后端+UI+引导） |
| B. Mac 标杆内嵌 | macOS 包内嵌 dsh 子进程（Electron 启动拉起，本机已验证 dsh 能跑）；win/linux 走引导 | 中 | Mac 完整闭环，win/linux 体验降级 |
| C. 全平台内嵌 | 四路都打包 dsh + node 运行时，Electron 全部拉起 | 大 | 高，需解决跨平台 node 运行时、体积、签名 |

**推荐 A**：最快、最稳，保证"四路安装包都能跑通"的验收标准成立；dsh 的完整本地体验以"本机装好即开箱"呈现。v0.2 再视反馈做 B/C。

---

## 四、执行里程碑（建议顺序）

1. **安全 + 仓库卫生**（不依赖构建，可立即动手）
2. **白屏 / dsh 接入**（核心体验，含引导卡 + 探测）
3. **构建链路**（PyInstaller 补依赖、dsh 策略落地、electron-builder、CI 加测试）
4. **P1 前端修复 + 文档回写**（agent-design 过时、checklist 全空、README 的 LangGraph 虚标）
5. **本地自测 + 建 remote + 推 `v0.1.0` tag** 触发 CI 四路构建与 GitHub Release

## 五、验收矩阵（自测）

- [ ] 装 DMG/包 → 后端 8765 起、对话页不白屏、能聊 `qwen2.5vl:7b`
- [ ] dsh 不可达时显示引导卡（非白屏）
- [ ] 绑定云端 API 后对话走云端兜底
- [ ] 设置/部署/硬件/API 四页正常
- [ ] 安全：从非本机源访问 `http://<ip>:8765` 被 CORS 拒绝
- [ ] win / mac / linux 四路 artifact 产出且可安装

## 六、风险与放宽建议

- "四路安装包跑通"建议定义为：**四路都能产出可安装文件 + 后端与 UI 可启动 + 对话页有引导**；dsh 完整闭环以 Mac 为标杆。不要为追"四路全内嵌 dsh"拖垮测试版排期。
- 缓存子系统（零实现）与 LangGraphScheduler（死代码）目前不阻塞发布，但**对外宣传应相应弱化**，避免被开源社区质疑。

---

## 七、发布实战经验（2026-08-31 实测）

- ✅ **结果**：张哥在本机 Terminal 推送 `main` + `v0.1.0` tag 成功上 `Near-Ink/LocalFlow-AI`，tag 触发四路 CI 构建。
- ⚠️ **agent 对话环境无法 `git push`**：WorkBuddy 注入的出口代理（`HTTP_PROXY=127.0.0.1:动态端口`）对 `CONNECT github.com:443` 回 `502/000`，git 隧道被挡死。**必须由用户在 Terminal 直连推**（Terminal 无该代理，已验证可传 1.9MB）。
- ⚠️ **fine-grained PAT 推含 `.github/workflows/*.yml` 的提交需 `Workflows: Read and write` 作用域**，否则 `remote rejected ... without workflow scope`。
- ⚠️ **fine-grained token 走 git over HTTPS 必须以 `x-access-token` 作用户名**（用账号名或 token 自身均 `403`）；classic token 可用账号名。
- ⚠️ **用 PAT 推送新建的 workflow 文件，GitHub 不会自动响应 push/tag 触发 run**（安全策略：第三方/PAT 创建的 workflow 需手动解锁）。推送后去 Actions 页 → Build & Release → Run workflow 手动跑一次解锁；解锁后重推 tag（`git tag -f v0.1.0 && git push --force origin v0.1.0`）才触发正式构建+Release。
- ⚠️ **electron-builder 未配置 `publish` 会导致四路打包统一失败**：报错 `Cannot read properties of null (reading 'provider')` @ `updateInfoBuilder.ts`。打包生成 update-info 时读 `publish.provider`，未配置且 CI 无法从 `.git` 推断 repository 即崩。修法：`desktop/package.json` 加 `repository` 字段 + `electron-builder.yml` 加 `publish: [{ provider: github }]`（配合 `build.yml` 的 `--publish never` 不上传，v0.1.0 不发自动更新）。
- 🔐 一次性 PAT 用完即去 GitHub → Settings → Developer settings → Personal access tokens 撤销。
