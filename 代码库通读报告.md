# LocalFlow AI 代码库通读报告

> 通读日期：2026-08-30 · 仓库：`/Users/zhanghaoran/Documents/LocalFlow AI`
> Git：2 次提交（`04397b9` 首版 → `af6593f` dsh 会话删除），**无 remote**（从未推送），工作区干净

> **发版后修正（2026-08-31）**：本报告为 v0.1.0 发版前快照。所列「缓存子系统零实现」「LangGraphScheduler 死代码」已在发版后修正——chat 结果缓存已接线到 SQLite（`namespace=llm`，见 `OllamaEngine.chat`），调度器更名为自研 `LLMSplitScheduler`（不依赖 LangGraph）；SSE token 估算也已统一口径。详见 commit `74c8052` / `1002de7` 与 `docs/dsh_compat.md`。

---

## 〇、通读范围与方法

**实际通读**：backend 源码 60 个 .py（7810 行）、desktop 前端单文件 5212 行、dsh-poc 全部源码与配置、docs 3 篇、3 个 HTML 交付物、workflow-community-source、CI 配置、运行日志（backend.log 11806 行 + nohup.out）。

**有意跳过**（附理由，非遗漏）：

| 跳过项 | 体积 | 理由 |
|---|---|---|
| `dsh-poc/node_modules/` | 270 MB | 第三方依赖 |
| `backend/dist/` + `backend/build/` | 90 MB | PyInstaller 产物，源码的副本 |
| `dsh-poc/dsh-home/sessions/*.zstd` | 14 个 | dsh 会话压缩日志 |
| `_shared/js/echarts.min.js` ×2、mermaid.min.js | 2.1 MB | 压缩后的第三方库 |
| `*.ttf` 字体、`.trae-html-share-packages/*.zip` | 7 MB | 二进制资源 |

有效通读量约 700 KB 文本。

---

## 一、项目全貌

**一句话**：自研 Python 后端（FastAPI，8765）+ Electron 壳，把对话与 Agent 能力整体外包给 DeepSeek Harness（dsh，8080），自己只保留四块差异化能力——**本地模型部署、硬件监控、对外 OpenAI 兼容 API、云端绑定**，再通过自研 dsh 工具插件 `file-workspace-preview` 把「文件工作区预览」注入 dsh 的 Agent 预设。

**端口分配**

| 端口 | 占用方 | 探测机制 |
|---|---|---|
| 8765 | Python FastAPI 后端 | 有探测，但逻辑宽松（2xx–4xx 都算在线） |
| 8080 | dsh Web UI（iframe 内嵌） | **完全没有探测** |
| 11434 | Ollama | 无 |

**架构落地情况**：Port & Adapter 六边形架构是真做了，依赖方向 `UI → API → Core → Ports ← Adapters` 基本守住了。

---

## 二、实际完成度矩阵（最关键的一节）

### ✅ 真跑通（有代码 + 有数据佐证）

| 能力 | 佐证 |
|---|---|
| Ollama chat / chat_stream / pull_stream | events.db 有真实对话记录，真 HTTP 调用 |
| ReAct Agent 循环 | `test_react_agent.py` 5/5 通过；events.db 有 `agent_final:6 / tool_call:7 / tool_result:7`；日志有 `POST /api/agent/run_stream 200` |
| 沙箱路径边界 | `test_sandbox.py` 8/8 通过（穿透/绝对路径/符号链接全拦） |
| OpenAI 兼容层 `/v1` | 鉴权联动有测试覆盖；日志中 `POST /v1/chat/completions` 被真实调用过 |
| 工作流 DAG 引擎 | 拓扑排序 + 环检测 + `{{nodeId.output}}` 占位符递归替换，逻辑完整 |
| 会话管理 | 事件溯源，`session_meta` 实现重命名/置顶 |
| 硬件识别库 | 四级分支 + 最长子串匹配；Apple 17 / NVIDIA 34 / AMD 9 / Intel Arc 4 / CPU 10 条 |
| 设置注册表 | 8 项设置，数据驱动——新增设置 Agent 工具自动获得读写能力，这个设计是对的 |
| 云端多绑定 | bind/unbind/switch + 连通性探测不污染激活态，写得不错 |

### ⚠️ 装了但从未接线（死代码）

| 项 | 证据 |
|---|---|
| **整个缓存子系统** | 全仓 grep `cache.get(`/`cache.set(` 生产代码零命中；`self.cache` 只在 app.py:104/106/150 赋值传递，session.py:29 存下后再无调用。运行时 cache.db **0 行**。「多层缓存 / 省 Token」卖点在代码层面**零实现** |
| **LangGraphScheduler** | app.py:114 装配后全仓无任何调用，无 API 暴露。**且根本不 import langgraph**，只是用 LLM 拆任务再串行跑，名字是营销（requirements.txt 里 langgraph 已被注释掉） |
| `OllamaEngine.pull()` | 阻塞版遗留，与 `pull_stream` 重复 |
| `db/` 包 | `__init__.py` 0 字节，纯占位 |
| 前端工作流画布 | 约 600 行完整 SVG DAG 编辑器，`initWF()` 被注释；**视图 HTML 已删除**，注释说「取消注释即可恢复」不成立 |
| 前端 `openAIShareGuide()` | 176 行完整实现，触发器 DOM 不存在，无任何入口 |

### ❌ 硬编码假数据 / stub

| 位置 | 问题 |
|---|---|
| `ollama_engine.py:97-101` | `get_context_length()` 里两个连续 `pass` 空语句，`num_ctx` 解析路径完全没实现 → `/api/context` 的 `context_size` 大概率恒为 0 |
| `openai_engine.py:94-96` | `list_models()` 硬编码返回单个模型 |
| `api/openai.py:264` | 流式 SSE 每 chunk `completion_tokens += 1` → **chunk 个数冒充 token 数** |
| `core/session.py:144-146` | `est = len(content)` → **字符数冒充 token 数**；导致非流式（真值）与流式（估值）不可比 |
| `core/app.py:175-177` | `async def shutdown(self): pass` —— httpx client 未 close、pull worker 未 cancel、sqlite 未关 |
| `ports/cache.py:6-7` | L3/L4 明写「预留，进阶版实现」 |

---

## 三、安全：一条真实的攻击链（P0，已逐行验证）

这是我亲自核实的，不是转述：

```
① 后端当前监听 *:8765（全网卡，PID 87744）
   $ lsof -nP -iTCP:8765 -sTCP:LISTEN  →  TCP *:8765 (LISTEN)
   但 openai-api-guide.html:237 白纸黑字写「默认后端只监听 127.0.0.1，仅本机进程可访问」
        → 文档与实现相反，用户被置于错误的安全认知中

② CORS = allow_origins=["*"] + allow_credentials=True（main.py:36-42）
   Starlette 在两者并存时走 cors.py:167-168 分支 → allow_explicit_origin()
        → 反射任意 Origin 并带上 Access-Control-Allow-Credentials: true
        → 效果等价于「允许所有来源的带凭据跨域请求」

③ API Key 默认不设置 = 完全免鉴权

④ Agent 工具有 11 个，含 file（读写）、shell（受限命令）、clipboard（读剪贴板）
```

**后果**：你浏览的任意网页，都能向 `http://127.0.0.1:8765` 发跨域请求并**读取响应**，进而驱动本机 Agent 的文件/命令能力。加上 0.0.0.0 监听，同局域网设备同样可达。

**最低修复**：`allow_origins` 改白名单（去掉 `*`）、默认绑定 `127.0.0.1`、文档改成与实际一致。

**其他安全问题**：
- `.npmrc` 设 `strict-ssl=false`（关闭 TLS 校验，供应链风险）
- dsh iframe 无 `sandbox` 属性、页面无 CSP meta
- macOS 产物未签名（CI 显式关掉），无公证、无校验和
- `patch-session-delete.sh` 直接改写 `node_modules` 内文件，`pnpm install` 即抹掉，且依赖 3 个精确文本锚点，dsh 升版本即 assert 失败

---

## 四、可用性：打包后必然白屏（P0）

`main.js` 只 spawn Python 后端，**从不启动 dsh**。而 `index.html:1082` 无条件设置 iframe src：

```js
_df.src = 'http://' + _dshHost + ':8080/?v=' + Date.now();
```

对话页的全部能力都在这个 iframe 里。**没有 8080 健康探测、没有 onerror 兜底、没有加载失败提示**。用户装完 DMG 双击打开，「对话」页就是一块白板且无从判断原因。

**其他 P0/P1 缺陷**：

| # | 问题 | 位置 |
|---|---|---|
| 1 | 相对路径 fetch 在 `file://` 下必挂 → 硬件页「缓存状态」永远显示统计不可用、Agent 产物预览全挂 | `index.html:4895, 4955, 4961, 4974` |
| 2 | `isBackendUp()` 把 4xx 也算在线 → 8765 被别的服务占用时会跳过启动自己的后端 | `main.js:20` |
| 3 | 首启向导 600ms 就弹，而后端就绪最长等 15s；且 `lf_wizard_seen` 只弹一次，第一次失败就再无引导 | `index.html:4519` |
| 4 | API Key 持久化抛 `PermissionError: ~/.localflow/config.json`，前端却用绿色 `ok` 提示成功，重启即丢 | 见 `ft-console-report.json` |
| 5 | 侧边栏与全部聊天控件 `display:none!important`，但 JS 仍在执行并做网络请求 | `index.html:769-777` |
| 6 | `sendAgent()` 在 `index.html:1995` 静默 return → 用户点「重跑」毫无反应 | |

---

## 五、文档与实现的脱节（P0）

| 文档说法 | 代码事实 |
|---|---|
| agent-design 断言「无工具调用 / 无 agent 运行时 / 无工具注册表」，把 `ports/tool.py`、`react_agent.py`、`sandbox.py` 列为待建 | **全部已存在并跑通**，运行时注册 11 个工具（设计文档 MVP 只规划 3 个） |
| checklist README 25 项任务 | **全部 `- [ ]` 未勾选**，但 A1–A6、B1–B3、C1–C3、D1 在代码与日志中均有实证 → 清单已失去进度追踪价值 |
| README「Python + FastAPI + LangGraph」 | 零 langgraph 依赖 |
| feasibility「简化硬件检测」 | 实际 `hardware_lib.py` 18KB + 独立 `system_hardware.py`，比建议的重得多 |

checklist 还存在**两份真相**：HTML 版存 localStorage（仅本机），README 版从不回写，必然长期不一致。

---

## 六、运行痕迹

后端**当前正在运行**（PID 87744）。backend.log 11806 行统计：**200 共 11790 次，404 共 2 次，ERROR/Traceback 计数 0**，最后三行是优雅退出，非崩溃。

| 次数 | 接口 | 说明 |
|---|---|---|
| 5284 / 5280 | `/api/models`、`/api/hardware/snapshot` | 前端定时轮询，合计占流量 99% |
| 1 | `POST /v1/chat/completions` | 对外 OpenAI API 被真实调用过 **1 次** |
| 0 | `/api/agent/run_stream` | 最新版本**从未回归验证 Agent**；而更早的 nohup.out 里有 5 次 |
| 3 + 3 | `POST /api/workspace`、`DELETE /api/workspace` | 建了 3 个全删掉，当前 `workspaces.json = []` |

`nohup.out`（早期版本）里 `/api/workspace/list` 返回 404，佐证该接口是后加的。

**测试覆盖**：`tests/` 三个文件（openai_key / react_agent / sandbox）真实有效，但**CI 里完全没有 lint 和测试步骤**。

---

## 七、其他问题清单

**P1 可移植性**
- `dsh-poc/dsh-home/profiles/*/package.json` 写死绝对路径 `link:/Users/zhanghaoran/Documents/LocalFlow AI/...`（含未转义空格）→ 换机器/上 CI 必然失败，POC 无法复现
- `publish.sh` 回退解释器写死 TRAE 内置路径 `/Users/zhanghaoran/Library/Application Support/TRAE SOLO CN/...`
- `pnpm-workspace.yaml` 是**未填写的占位**：5 个 `allowBuilds` 值全是字面量 `set this to true or false`（含需原生编译的 koffi、node-pty）
- `pypdf` 在 `api/attach.py:32` 被 import，但 requirements.txt、pyproject.toml、spec 的 collect_all **三处都没声明** → 打包后 PDF 解析必 ImportError

**P1 未打通的链路**
- 社区模板源 manifest 的 5 个 url 仍是 `https://youruser.github.io/...` 占位域名；实时 `/api/workflow/sources` 只返回 `builtin`，**整条链路从未生效**
- 社区源 5 个模板 = 内置模板的镜像，`build_manifest.py` 从 `BUILTIN_TEMPLATES` 导出，无增量价值
- `launcher/` 是空目录（只有 .DS_Store）

**P2 工程质量**
- `<mark.key>` 是非法标签名（应为 `<mark class="key">`），共 4 处：feasibility 187/297/424、checklist 117。CSS 里写的是 `mark.key{...}`，浏览器会当成未知元素，高亮不生效
- `backend.log`（792KB）被 git 跟踪，`.gitignore` 只忽略 `*.db` 没忽略 `*.log`
- `agent-design.html:337` 孤立 `</code>`；`:350` 空 `<ul class="structural">`（class 还拼错）
- `hardware-monitor-prototype.html` 底部写「每秒刷新」，实际 `setInterval(tick, 1500)`；硬件数据（RTX 4060/32GB）是硬编码虚构，与本机 Apple M4 不符
- `sync-localflow-models.mjs` 读了 `vision` 字段却从不使用；`:93` 用 `JSON.stringify` 比较做变更检测，键序敏感会导致反复写配置
- CI 在 4 个 matrix 任务里各重新 PyInstaller 打包一次，无跨任务缓存
- `upload-artifact` path 含 `*.deb`，但 electron-builder 的 linux target 只有 AppImage，通配无效

---

## 八、结论

**架构是对的，工程完成度被高估了。**

做对的三件事：Port & Adapter 真的落地了；「不 fork dsh、只做工具插件注入」这个决策很聪明——用 104 行 `file-workspace-preview` 换来了完整 Agent 能力；硬件识别库的四级兜底确实做到了"绝不因识别失败中断部署"。

需要正视的三个事实：
1. **两个核心卖点是空的**——缓存零实现、LangGraph 零依赖。对外宣传应相应调整。
2. **打包后对话页必然白屏**，这是当前最大的可用性缺口，比任何架构问题都紧急。
3. **安全风险已验证成立**（0.0.0.0 + CORS 全开 + 默认免鉴权 + Agent 有文件/命令工具），建议优先于任何新功能处理。

**建议的修复顺序**：安全三件套（绑定/CORS/文档一致）→ Electron 拉起 dsh + 8080 探测 → 相对路径 fetch → 缓存接线 or 从宣传中撤下 → 回写 checklist 与 agent-design 文档。
